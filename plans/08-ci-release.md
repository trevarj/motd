# 08 — CI and release (historical pointer)

This original implementation plan has been retired because the checked-in
workflow evolved after it was written.

- Current CI and release behavior: `.github/workflows/`
- Agent release procedure and recovery: `.agents/releases.md`
- Local verification commands: `.agents/testing.md`
- Public build and release overview: `README.md`

Git history retains the original plan. Do not copy workflow YAML, secret policy,
or release gates from an older revision into the current repository.
