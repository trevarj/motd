name: Fast headless E2E (hermetic)

on:
  workflow_call:
  workflow_dispatch:

jobs:
  soju-vless-reality:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      MOTD_STACK_DIR: ${{ runner.temp }}/motd-vless-reality
      # This is a socket-level proof; no Android device is involved.
      MOTD_SKIP_ADB_REVERSE: "1"
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Nix tooling
        uses: DeterminateSystems/nix-installer-action@v22

      - name: Prove Soju history through VLESS + REALITY
        run: |
          ./test/e2e/local-stack.sh up
          ./test/e2e/local-stack.sh history-check
          ./test/e2e/local-stack.sh obfs-xray-up
          ./test/e2e/local-stack.sh obfs-xray-validate
          ./test/e2e/local-stack.sh obfs-xray-history-check
          ./test/e2e/local-stack.sh obfs-xray-negative

      - name: Upload proxy-stack diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: soju-vless-reality-diagnostics
          path: ${{ runner.temp }}/motd-vless-reality/**/*.log
          if-no-files-found: ignore

      - name: Tear down proxy stack
        if: always()
        run: |
          ./test/e2e/local-stack.sh obfs-xray-down || true
          ./test/e2e/local-stack.sh down || true

  fast-headless:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: 17 }
      - uses: gradle/actions/setup-gradle@v4

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Start hermetic soju and ergo
        run: docker compose -f test/e2e/hermetic/docker-compose.yml up --build -d --wait

      - name: Run isolated Kotlin headless journeys
        run: |
          ./gradlew headlessApi34FossE2eAndroidTest \
            -Pandroid.testoptions.manageddevices.emulator.gpu=swiftshader_indirect \
            -Pandroid.testInstrumentationRunnerArguments.annotation=io.github.trevarj.motd.FastHeadlessE2e \
            -Pandroid.testInstrumentationRunnerArguments.sojuHost=10.0.2.2 \
            -Pandroid.testInstrumentationRunnerArguments.sojuPort=6697 \
            -Pandroid.testInstrumentationRunnerArguments.sojuUser=motd \
            -Pandroid.testInstrumentationRunnerArguments.sojuPassword=motdtest \
            -Pandroid.testInstrumentationRunnerArguments.nick=motdadb \
            -Pandroid.testInstrumentationRunnerArguments.channel=##motdtest \
            -Pandroid.testInstrumentationRunnerArguments.secondNick=motdadb2 \
            --stacktrace

      - name: Upload smoke reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fast-headless-e2e-reports
          path: |
            app/build/reports/androidTests/**
            app/build/outputs/androidTest-results/**
          if-no-files-found: ignore

      - name: Dump bouncer logs on failure
        if: failure()
        run: docker compose -f test/e2e/hermetic/docker-compose.yml logs --no-color || true

      - name: Tear down hermetic stack
        if: always()
        run: docker compose -f test/e2e/hermetic/docker-compose.yml down -v || true
